<!DOCTYPE html>
<html>
<head>
  <title>TDD</title>
  <meta charset="utf-8">
</head>
<body>
  <div class="title">
    <h1>Todo Doing Done</h1>
  </div>
  <h2>プロジェクト一覧</h2>
  <%= form_for @project, :url => '/yeah' do |f| %>
    <%= f.label :title, "プロジェクト新規作成：" %>
    <%= f.text_field :title %>
    <%= f.submit "作成" %>
  <% end %>

<nav>
  <ul>
    <% @projects.each do |project| %>
        <li><input type="checkbox" name="" value=""><%= link_to project.title, projects_path+"/"+project.id.to_s%></li>
    <% end %>
</nav>
</body>
</html>
